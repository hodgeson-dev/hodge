meta:
  version: "1.0.0"
  api_style: rest
  framework: fastify
  version_range: ">=3.0.0 <5.0.0"
  detection:
    deps: ["fastify"]
  applies_to:
    - "**/*.ts"
    - "**/*.js"
    - "**/routes/**"
    - "**/handlers/**"

rules:
  - id: placeholder
    name: "Placeholder Rule"
    enforcement: SUGGESTED
    severity: SUGGESTION
    desc: "Profile content coming in future version based on user demand"
